gnome.compile_schemas(
  build_by_default: true,
  depend_files: ['io.github.moon.Nyx.gschema.xml']
)

install_data('io.github.moon.Nyx.gschema.xml',
  install_dir: join_paths(prefix, datadir, 'glib-2.0', 'schemas')
)
